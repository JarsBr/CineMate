<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/iconePlataforma"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginStart="5dp"
        android:scaleType="centerCrop"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.App.CornerSize8dp" />

    <CheckBox
        android:id="@+id/checkboxItem"
        android:layout_width="34dp"
        android:layout_height="48dp"
        android:buttonTint="#FF0000"
        android:padding="0dp"
        android:theme="@style/ThemeOverlay.AppCompat"
        app:layout_constraintBottom_toBottomOf="@+id/iconePlataforma"
        app:layout_constraintStart_toEndOf="@+id/iconePlataforma"
        app:layout_constraintTop_toTopOf="@+id/iconePlataforma"
        app:layout_constraintVertical_bias="0.0" />

    <TextView
        android:id="@+id/nomePlataforma"
        android:layout_width="137dp"
        android:layout_height="23dp"
        android:textColor="#000000"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toEndOf="@+id/checkboxItem"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.52" />
</androidx.constraintlayout.widget.ConstraintLayout>
